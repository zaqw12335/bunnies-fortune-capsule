import React from "react";
import { createRoot } from "react-dom/client";
import BunniesFortuneCapsule from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BunniesFortuneCapsule />
  </React.StrictMode>
);
